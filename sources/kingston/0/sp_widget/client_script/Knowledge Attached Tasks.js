function($rootScope, $scope) {
  /* widget controller */
  var c = this;
  
	//c.options.title = c.options.title || $rootScope.messages.ATTACHED_INCIDENTS;
	
	/*c.max_display_count = c.options.max_display_count ? c.options.max_display_count * 1 : c.data.maxCountProperty;
	if(c.max_display_count < 0){
		c.max_display_count = 10;
	}
	c.attachedIncidents = $rootScope.attachedIncidents ? $rootScope.attachedIncidents.slice(0, c.max_display_count) : []; 	
*/
}